radix-4 fusion acceptance attempt, second session: acceptance needs ONE session that reaches all 20 cases byte-exact and sessions die stochastically at 6-17 requests, so a second concurrent attempt is the cheapest route to a gated verdict (board 3 runs the same engine in parallel as its own attempt). Request timeout cut to 200 s so a wedged session reports in ~25 min instead of 2 h.