Extended-group reading for the accepted-candidate bundle (engine a800d1ea1896 + asm-inclusive provenance). Its primary, think, boot_bench, prefill, min_case, token_delta and both heaps are measured on the image; ext_decode_tps is still carried from run #395 because every full suite died in the extended tail. Thirteen requests from a fresh boot fit the measured 16-20 request per-boot budget, so this completes the candidate's monitor set for the owner report. Groups restricted, so measure.sh skips the device byte-exact gate by design and this is explicitly NOT an acceptance attempt; no prompt, golden, gate or metric touched.
