Radix-4 fusion engine 8667e160a278 measured with AUTO_GROUPS=primary,think: this is NOT another primary repeat. The fusion's accepted-grade record is missing think_tps, boot_bench_tps, internal_free and psram_free because every prior session died in the extended tail, so those fields have been carried from run #395 rather than measured. Seven requests fit inside the measured 16-20 per-boot budget, so this run produces the candidate's own monitor set. Pool is otherwise idle, which the stall data says buys the furthest case reach.
