[
  {
    "name": "get_status",
    "description": "Read device uptime, free memory, telemetry and timer status.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "set_sampling_interval",
    "description": "Set how often the device samples telemetry, in seconds.",
    "parameters": {
      "type": "object",
      "properties": {
        "seconds": {
          "type": "integer",
          "minimum": 1,
          "maximum": 3600
        }
      },
      "required": [
        "seconds"
      ]
    }
  },
  {
    "name": "set_timer",
    "description": "Start a countdown timer for the given number of seconds.",
    "parameters": {
      "type": "object",
      "properties": {
        "seconds": {
          "type": "integer",
          "minimum": 1,
          "maximum": 3600
        }
      },
      "required": [
        "seconds"
      ]
    }
  }
]
